# Luotea Reliability Risk Engine — pinned to the shared luotea-pipeline venv (Python 3.14).
# Install:  python -m venv .venv && source .venv/bin/activate && pip install -r requirements.txt
polars==1.41.2
pyarrow>=15
pandas>=2.2
scikit-learn==1.9.0
matplotlib==3.10.9
altair==6.2.1
streamlit==1.58.0
pytest>=8
